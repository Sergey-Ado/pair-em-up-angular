export enum Constants {
  GITHUB = 'https://github.com/sergey-ado',
  REMOVE_DELAY = 300,
  HINT_DELAY = 1000,
}

export enum Pages {
  START = 'start',
  GAME = 'game',
  HIGH_SCORE = 'high-score',
}

export enum Modes {
  CLASSIC = 'classic',
  RANDOM = 'random',
  CHAOTIC = 'chaotic',
}
