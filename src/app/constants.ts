export enum Constants {
  GITHUB = 'https://github.com/sergey-ado',
}

export enum Pages {
  START = 'start',
  GAME = 'game',
  HIGHSCORE = 'high-score',
}

export enum Modes {
  CLASSIC = 'classic',
  RANDOM = 'random',
  CHAOTIC = 'chaotic',
}
